// if(fval)//fval to bool
// dval = fval + ival;//ival is conveted to float and the result is converted to double
// dval + ival * cval;//cval is converted to int and the result is converted to double
