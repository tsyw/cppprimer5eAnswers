// char cval; int ival; unsigned int ui; float fval; double dval;
// //(a): cval = 'a' + 3;//'a' to int, the result to char
// //(b): fval = ui - ival * 1.0;//ival to double, ui to double , the result to float
// //(c): dval = ui * fval;//ui to float, the to double
// //(d): cval = ival + fval + dval;//ival to float, the result and fval to double, then to char
