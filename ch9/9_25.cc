// if elem1 equals to elem2, then no objects will be deleted
// if elem2 == off the end iterator, then it will delete the objects from elem1 to the end
// if elem1 == elem2 == off the end iterator, then nothing will be deleted
