// vector<string> svec;
// svec.reserve(1024);// allocate 1024 spaces
// string word;
// while (cin >> word)
//     svec.push_back(word);// read strings and store in the svec
// svec.resize(svec.size()+svec.size()/2);// resize the svec to add size/2 more empty string into the svec
