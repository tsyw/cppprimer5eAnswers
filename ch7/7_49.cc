// s is converted to Sales_data implicitly, and then copy to the Sales_data object.
// error, we cannot convert a temporary object to a reference.
// error, const function, cannot change the *this's members.
