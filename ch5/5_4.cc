// (a): while(string::iterator iter != s.end()){/**/}//incorrect, it will define a new iterator after each loop. and it does not get an initial number.
// (b): while(bool status = find(word)){/**/}
// 		if(!status){/**/}//!status is always true
